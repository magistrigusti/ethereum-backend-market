import { ethers } from 'ethers';
import { useEffect, useState } from 'react';

import close from '../images/logo/logo.png';

const Home = ({ home, provider, escrow, togglePop }) => {

    return (
        <div className="home">
        </div>
    );
}

export default Home;
